spring:
  profiles:
    active: dev
  application:
    name: stargaze-metric
  main:
    web-application-type: servlet

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
