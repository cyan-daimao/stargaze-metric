spring:
  config:
    import:
      - "nacos:arch-base.yaml?group=DEFAULT_GROUP&namespace=pre"
      - "nacos:stargaze-metric.yaml?group=DEFAULT_GROUP&namespace=pre"
  cloud:
    nacos:
      server-addr: 10.0.0.2:8848
      username: nacos
      password: nacos
      config:
        file-extension: yaml
        namespace: pre
      discovery:
        ephemeral: true
        namespace: pre
  # 元数据库 PostgreSQL(观星元数据 + pgvector 向量)
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://10.0.0.2:5432/stargaze?currentSchema=public
    username: postgres
    password: postgres
  # MyBatis-Plus
  data:
    redis:
      host: 10.0.0.2
      port: 6379

# 跨服务调用 dataset
feign:
  stargaze-dataset:
    url: http://stargaze-dataset
