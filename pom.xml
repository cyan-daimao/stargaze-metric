<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.cyan</groupId>
        <artifactId>arch</artifactId>
        <version>1.0-SNAPSHOT</version>
        <relativePath/>
    </parent>

    <groupId>com.cyan</groupId>
    <artifactId>stargaze-metric</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <description>观星-指标平台:指标/维度升级 + 多数据集绑定 + resolve(聚合工程)</description>

    <modules>
        <module>stargaze-metric-client</module>
        <module>stargaze-metric-application</module>
    </modules>

    <properties>
        <arch.version>1.0-SNAPSHOT</arch.version>
        <cyan-employee-login.version>1.0-SNAPSHOT</cyan-employee-login.version>
        <stargaze-dataset-client.version>1.0-SNAPSHOT</stargaze-dataset-client.version>
        <postgresql.version>42.7.4</postgresql.version>
        <fastjson2.version>2.0.52</fastjson2.version>
        <mybatis-plus.version>3.5.7</mybatis-plus.version>
        <mapstruct.version>1.5.5.Final</mapstruct.version>
        <lombok.version>1.18.42</lombok.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.cyan</groupId>
                <artifactId>arch-common</artifactId>
                <version>${arch.version}</version>
            </dependency>
            <dependency>
                <groupId>com.cyan</groupId>
                <artifactId>arch-base</artifactId>
                <version>${arch.version}</version>
            </dependency>
            <dependency>
                <groupId>com.cyan</groupId>
                <artifactId>cyan-employee-login</artifactId>
                <version>${cyan-employee-login.version}</version>
            </dependency>
            <dependency>
                <groupId>com.cyan</groupId>
                <artifactId>stargaze-dataset-client</artifactId>
                <version>${stargaze-dataset-client.version}</version>
            </dependency>
            <dependency>
                <groupId>com.cyan</groupId>
                <artifactId>stargaze-metric-client</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.alibaba.fastjson2</groupId>
                <artifactId>fastjson2</artifactId>
                <version>${fastjson2.version}</version>
            </dependency>
            <dependency>
                <groupId>org.postgresql</groupId>
                <artifactId>postgresql</artifactId>
                <version>${postgresql.version}</version>
            </dependency>
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <source>21</source>
                        <target>21</target>
                        <compilerArgs>
                            <arg>-parameters</arg>
                        </compilerArgs>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                            <path>
                                <groupId>org.mapstruct</groupId>
                                <artifactId>mapstruct-processor</artifactId>
                                <version>${mapstruct.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <distributionManagement>
        <repository>
            <id>maven-releases</id>
            <name>Nexus Releases Repository</name>
            <url>http://nexus.cyan.com/repository/maven-releases/</url>
        </repository>
        <snapshotRepository>
            <id>maven-snapshots</id>
            <name>Nexus Snapshots Repository</name>
            <url>http://nexus.cyan.com/repository/maven-snapshots/</url>
        </snapshotRepository>
    </distributionManagement>
</project>
